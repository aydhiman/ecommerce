```handlebars
<h1 style="margin-bottom: 2rem;">My Orders</h1>

{{
#if
 orders.length}}
    {{
#each
 orders}}
    <div class="card" style="margin-bottom: 2rem;">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px solid #eee;">
            <div>
                <h3>Order #{{this._id}}</h3>
                <p style="color: #666;">{{formatDate this.createdAt}}</p>
            </div>
            <div style="text-align: right;">
                <p style="font-size: 1.2rem; font-weight: bold;">${{this.totalPrice}}</p>
                <span style="padding: 0.3rem 1rem; border-radius: 15px; font-size: 0.9rem; 
                             {{
#if
 (eq this.status 'pending')}}background: #fff3cd; color: #856404;{{
/if
}}
                             {{
#if
 (eq this.status 'confirmed')}}background: #d1ecf1; color: #0c5460;{{
/if
}}
                             {{
#if
 (eq this.status 'shipped')}}background: #d4edda; color: #155724;{{
/if
}}
                             {{
#if
 (eq this.status 'delivered')}}background: #d4edda; color: #155724;{{
/if
}}
                             {{
#if
 (eq this.status 'cancelled')}}background: #f8d7da; color: #721c24;{{
/if
}}">
                    {{this.status}}
                </span>
            </div>
        </div>
        
        <h4 style="margin-bottom: 1rem;">Items:</h4>
        {{
#each
 this.items}}
        <div style="display: flex; gap: 1rem; margin-bottom: 1rem; padding: 1rem; background: #f8f9fa; border-radius: 8px;">
            <img src="/uploads/{{this.product.image}}" alt="{{this.product.name}}" 
                 style="width: 60px; height: 60px; object-fit: cover; border-radius: 4px;"
                 onerror="this.src='/uploads/default-product.jpg'">
            <div>
                <h5>{{this.product.name}}</h5>
                <p style="color: #666;">Quantity: {{this.quantity}} × ${{this.price}} = ${{multiply this.quantity this.price}}</p>
            </div>
        </div>
        {{
/each
}}
        
        <div style="margin-top: 1rem; padding-top: 1rem; border-top: 1px solid #eee;">
            <p><strong>Shipping Address:</strong> {{this.shippingAddress}}</p>
        </div>
    </div>
    {{
/each
}}
{{else}}
    <div class="card" style="text-align: center; padding: 3rem;">
        <h2>No orders yet</h2>
        <p style="margin: 1rem 0; color: #666;">You haven't placed any orders yet.</p>
        <a href="/products-list" class="btn btn-primary">Start Shopping</a>
    </div>
{{
/if
}}
```