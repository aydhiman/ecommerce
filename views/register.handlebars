```handlebars
<div style="max-width: 500px; margin: 2rem auto;">
    <h1 style="text-align: center; margin-bottom: 2rem;">Register</h1>
    
    <div style="display: flex; gap: 1rem; margin-bottom: 2rem; justify-content: center;">
        <button onclick="showUserRegister()" id="userTab" class="btn btn-primary">User Register</button>
        <button onclick="showSellerRegister()" id="sellerTab" class="btn btn-secondary">Seller Register</button>
    </div>
    
    <!-- User Registration Form -->
    <form method="POST" action="/auth/register" id="userRegister">
        <h2 style="margin-bottom: 1.5rem; text-align: center;">User Registration</h2>
        <div class="form-group">
            <label for="name">Full Name:</label>
            <input type="text" name="name" required placeholder="Enter your full name">
        </div>
        <div class="form-group">
            <label for="phone">Phone Number:</label>
            <input type="text" name="phone" required placeholder="Enter your phone number">
        </div>
        <div class="form-group">
            <label for="address">Address:</label>
            <textarea name="address" required placeholder="Enter your full address" rows="3"></textarea>
        </div>
        <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" name="password" required placeholder="Enter your password (min 6 characters)">
        </div>
        <button type="submit" class="btn btn-primary" style="width: 100%;">Register as User</button>
    </form>
    
    <!-- Seller Registration Form -->
    <form method="POST" action="/auth/seller/register" id="sellerRegister" style="display: none;">
        <h2 style="margin-bottom: 1.5rem; text-align: center;">Seller Registration</h2>
        <div class="form-group">
            <label for="name">Business Name:</label>
            <input type="text" name="name" required placeholder="Enter your business name">
        </div>
        <div class="form-group">
            <label for="email">Email:</label>
            <input type="email" name="email" required placeholder="Enter your email">
        </div>
        <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" name="password" required placeholder="Enter your password (min 6 characters)">
        </div>
        <button type="submit" class="btn btn-primary" style="width: 100%;">Register as Seller</button>
    </form>
    
    <div style="text-align: center; margin-top: 2rem;">
        <p>Already have an account? <a href="/login">Login here</a></p>
    </div>
</div>

<script>
function showUserRegister() {
    document.getElementById('userRegister').style.display = 'block';
    document.getElementById('sellerRegister').style.display = 'none';
    document.getElementById('userTab').className = 'btn btn-primary';
    document.getElementById('sellerTab').className = 'btn btn-secondary';
}

function showSellerRegister() {
    document.getElementById('userRegister').style.display = 'none';
    document.getElementById('sellerRegister').style.display = 'block';
    document.getElementById('userTab').className = 'btn btn-secondary';
    document.getElementById('sellerTab').className = 'btn btn-primary';
}
</script>
```

### views/cart.handlebars
```handlebars
<h1 style="margin-bottom: 2rem;">Shopping Cart</h1>

{{
#if
 cart.items.length}}
    <div class="card">
        {{
#each
 cart.items}}
        <div style="display: flex; gap: 2rem; padding: 1.5rem; border-bottom: 1px solid #eee; align-items: center;">
            <img src="/uploads/{{this.product.image}}" alt="{{this.product.name}}" 
                 style="width: 80px; height: 80px; object-fit: cover; border-radius: 8px;"
                 onerror="this.src='/uploads/default-product.jpg'">
            
            <div style="flex: 1;">
                <h3 style="margin-bottom: 0.5rem;">{{this.product.name}}</h3>
                <p style="color: #666; margin-bottom: 0.5rem;">{{this.product.description}}</p>
                <p style="font-weight: bold; color: #4CAF50;">${{this.product.price}} each</p>
            </div>
            
            <div style="display: flex; align-items: center; gap: 1rem;">
                <form method="POST" action="/cart/update" style="display: flex; align-items: center; gap: 0.5rem;">
                    <input type="hidden" name="productId" value="{{this.product._id}}">
                    <label>Qty:</label>
                    <input type="number" name="quantity" value="{{this.quantity}}" min="1" 
                           style="width: 60px; padding: 0.3rem;" onchange="this.form.submit()">
                </form>
                
                <p style="font-weight: bold; min-width: 80px;">${{multiply this.product.price this.quantity}}</p>
                
                <form method="POST" action="/cart/remove">
                    <input type="hidden" name="productId" value="{{this.product._id}}">
                    <button type="submit" style="background: #dc3545; color: white; border: none; padding: 0.3rem 0.8rem; border-radius: 4px; cursor: pointer;">Remove</button>
                </form>
            </div>
        </div>
        {{
/each
}}
        
        <div style="padding: 1.5rem; background: #f8f9fa; text-align: right;">
            <h2 style="margin-bottom: 1rem;">Total: ${{totalPrice}}</h2>
            <form method="POST" action="/user/order">
                <button type="submit" class="btn btn-primary" style="font-size: 1.1rem; padding: 1rem 2rem;">Place Order</button>
            </form>
        </div>
    </div>
{{else}}
    <div class="card" style="text-align: center; padding: 3rem;">
        <h2>Your cart is empty</h2>
        <p style="margin: 1rem 0; color: #666;">Add some products to your cart to get started!</p>
        <a href="/products-list" class="btn btn-primary">Continue Shopping</a>
    </div>
{{
/if
}}
```